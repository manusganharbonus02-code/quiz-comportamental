{
  "name": "quiz-comportamental-app-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root monorepo for the AI Behavioral Profile Quiz",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "NODE_ENV=production node server/server.js",
    "dev": "concurrently \"npm:dev --prefix client\" \"node --watch server/server.js\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
